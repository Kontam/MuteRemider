export const ACTION_CHANGE_BASE_URL = 'CHANGE_BASE_URL';

const baseUrl = (state = '', action) => {
  switch (action.type) {
  case TwAppsConst.ACTION_CHANGE_BASE_URL:
    return action.baseUrl;
  default:
    return state;
  }
};

export default baseUrl;
